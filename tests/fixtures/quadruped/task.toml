# Task IR (spec 6) for the MuJoCo Playground Go1 joystick task -- packet M6/B1.
#
# Generated by `cargo test -p es --test cli -- --ignored regenerate_quadruped_documents` from
# tests/fixtures/mjcf/go1_primitives.xml, so `scene_hash`, `asset_hash`, the reset pose and
# the twelve joint names are derived, never typed in. Upstream's parameters are pinned in
# docs/api-notes/mujoco-playground-quadruped.md; the design note is
# docs/design/quadruped-track.md.
#
# Track B, and this is the whole framing: the policy is trained *externally* by
# `mujoco_playground` (JAX/MJX PPO, 200 M steps) and imported as weights (spec 8, spec 1.9 --
# we do not build a second training stack). So this document is not what trains anything. It
# is (a) the scene, reset and termination contract our runtime executes the imported policy
# under, and (b) the declaration of the observation channel the Observation IR implements.
#
# Parameters: NJ = 12, control 50 Hz (`ctrl_dt = 0.02`), max_episode_steps = 1000 (20 s,
# upstream's `episode_length`), action = 12 joint position targets.
#
# `state`, the one declared channel, is Playground's 48-wide policy input in its exact order:
#
#     local_linvel(3, m/s)  gyro(3, rad/s)  gravity(3, unit vector)
#     joint_pos(12, rad, minus default_pose)  joint_vel(12, rad/s)
#     last_action(12, dimensionless)  command(3, [m/s, m/s, rad/s])
#
# The port's unit is `Dimensionless` because that vector is mixed and spec 5.4's algebra has
# no mixed unit -- the block units are the list above, and this comment is where they live.
# Upstream's per-channel additive uniform observation noise is applied inside the trainer's
# env; it is not represented here, and `AugmentKind` has no uniform variant to represent it
# with (INV-15 would keep it off during evaluation anyway).
#
# THREE CEILINGS, recorded rather than papered over (design note section 3):
#
#  * The `ObservationSpec` node is deliberately UNFED. No IR-D source produces base linear
#    velocity in the body frame, projected gravity, the previous action or the joystick
#    command, so there is no graph value to bind to it. Declaring the channel is still
#    correct -- spec 7.4 says Task IR declares and Observation IR implements -- but nothing in
#    IR-D computes this vector, and `es-eval`'s capture path cannot serve it either: it reads
#    `qpos` slices and MJCF sensors, and this scene has neither the velocities nor (because
#    `mjcf_out` emits only jointpos/jointvel) the sensors. That is the track's first blocking
#    item, and `es eval run` names it rather than faking a run.
#  * Termination is timeout plus "the base left the arena" (|x| > 3 m). Upstream terminates
#    on the upright vector's z-component going negative, and the usual second guard is base
#    height -- neither is expressible: `es-env`'s reward/termination cone binds a joint's
#    FIRST qpos index (crates/es-env/src/plan.rs `joint_leaf`), and a free joint's first index
#    is x, not z and not a quaternion component. (Its unit here is `Angle`, not `Length`:
#    `JointQuantity::Position.unit()` is per-quantity, not per-joint-kind, and a free joint's
#    translational coordinates come out typed as if they were hinge angles.)
#  * The reward is forward speed normalized over the command bound, not upstream's
#    `exp(-err^2 / tracking_sigma)`: `MathFn` is not in the cone and IR-D has no constant leaf
#    to subtract a commanded velocity with. Upstream's fifteen shaped terms train the policy
#    in `mujoco_playground`; this one term is for *our* evaluation harness.
#
# The thirteen `ResetState` nodes are load-bearing, not decorative: `SceneDesc` carries no
# keyframe, so without them the runtime resets to the XML's `qpos0` -- every hinge at 0, the
# trunk at z = 0.445 -- and `default_pose`, the zero-point of every action, never reaches the
# simulator.
es_schema = 1
kind = "task"

[body]
schema_version = 2

[body.config]
control_rate_hz = 50.0
deterministic = true
max_episode_steps = 1000
rng_streams = [
    "command.ang_z",
    "command.lin_x",
    "command.lin_y",
    "reset.FL_calf_joint",
    "reset.FL_hip_joint",
    "reset.FL_thigh_joint",
    "reset.FR_calf_joint",
    "reset.FR_hip_joint",
    "reset.FR_thigh_joint",
    "reset.RL_calf_joint",
    "reset.RL_hip_joint",
    "reset.RL_thigh_joint",
    "reset.RR_calf_joint",
    "reset.RR_hip_joint",
    "reset.RR_thigh_joint",
    "reset.base_z",
]

[body.graph]
inputs = []
outputs = []
schema_version = 2

[[body.graph.edges]]

[body.graph.edges.from]
node = 0
port = "value"

[body.graph.edges.to]
node = 1
port = "a"

[[body.graph.edges]]

[body.graph.edges.from]
node = 1
port = "value"

[body.graph.edges.to]
node = 2
port = "value"

[[body.graph.edges]]

[body.graph.edges.from]
node = 3
port = "value"

[body.graph.edges.to]
node = 4
port = "a"

[[body.graph.edges]]

[body.graph.edges.from]
node = 4
port = "value"

[body.graph.edges.to]
node = 5
port = "value"

[[body.graph.edges]]

[body.graph.edges.from]
node = 6
port = "value"

[body.graph.edges.to]
node = 7
port = "value"

[[body.graph.edges]]

[body.graph.edges.from]
node = 7
port = "value"

[body.graph.edges.to]
node = 8
port = "value"

[body.graph.nodes.0.GetTime]
since_reset = true

[body.graph.nodes.1.Compare]
op = "Ge"
rhs = 20.0

[body.graph.nodes.1.Compare.ty]
elem = "F32"
frame = "World"
shape = [1]
time = "Tick"
unit = "Time"

[body.graph.nodes.10.ObservationSpec]
channel = "state"

[body.graph.nodes.10.ObservationSpec.ty]
elem = "F32"
frame = "World"
shape = [48]
time = "Tick"
unit = "Dimensionless"

[body.graph.nodes.11.Randomization]
stream = "command.lin_x"
target = "command.lin_x"

[body.graph.nodes.11.Randomization.dist.Uniform]
hi = 1.5
lo = -1.5

[body.graph.nodes.12.Randomization]
stream = "command.lin_y"
target = "command.lin_y"

[body.graph.nodes.12.Randomization.dist.Uniform]
hi = 0.8
lo = -0.8

[body.graph.nodes.13.Randomization]
stream = "command.ang_z"
target = "command.ang_z"

[body.graph.nodes.13.Randomization.dist.Uniform]
hi = 1.2
lo = -1.2

[body.graph.nodes.14.ResetState]
stream = "reset.base_z"
target = "qpos[2]"

[body.graph.nodes.14.ResetState.dist]
Constant = 0.278

[body.graph.nodes.15.ResetState]
stream = "reset.FR_hip_joint"
target = "joint.FR_hip_joint.qpos"

[body.graph.nodes.15.ResetState.dist]
Constant = 0.1

[body.graph.nodes.16.ResetState]
stream = "reset.FR_thigh_joint"
target = "joint.FR_thigh_joint.qpos"

[body.graph.nodes.16.ResetState.dist]
Constant = 0.9

[body.graph.nodes.17.ResetState]
stream = "reset.FR_calf_joint"
target = "joint.FR_calf_joint.qpos"

[body.graph.nodes.17.ResetState.dist]
Constant = -1.8

[body.graph.nodes.18.ResetState]
stream = "reset.FL_hip_joint"
target = "joint.FL_hip_joint.qpos"

[body.graph.nodes.18.ResetState.dist]
Constant = -0.1

[body.graph.nodes.19.ResetState]
stream = "reset.FL_thigh_joint"
target = "joint.FL_thigh_joint.qpos"

[body.graph.nodes.19.ResetState.dist]
Constant = 0.9

[body.graph.nodes.2.Terminate]
kind = "Timeout"

[body.graph.nodes.20.ResetState]
stream = "reset.FL_calf_joint"
target = "joint.FL_calf_joint.qpos"

[body.graph.nodes.20.ResetState.dist]
Constant = -1.8

[body.graph.nodes.21.ResetState]
stream = "reset.RR_hip_joint"
target = "joint.RR_hip_joint.qpos"

[body.graph.nodes.21.ResetState.dist]
Constant = 0.1

[body.graph.nodes.22.ResetState]
stream = "reset.RR_thigh_joint"
target = "joint.RR_thigh_joint.qpos"

[body.graph.nodes.22.ResetState.dist]
Constant = 0.9

[body.graph.nodes.23.ResetState]
stream = "reset.RR_calf_joint"
target = "joint.RR_calf_joint.qpos"

[body.graph.nodes.23.ResetState.dist]
Constant = -1.8

[body.graph.nodes.24.ResetState]
stream = "reset.RL_hip_joint"
target = "joint.RL_hip_joint.qpos"

[body.graph.nodes.24.ResetState.dist]
Constant = -0.1

[body.graph.nodes.25.ResetState]
stream = "reset.RL_thigh_joint"
target = "joint.RL_thigh_joint.qpos"

[body.graph.nodes.25.ResetState.dist]
Constant = 0.9

[body.graph.nodes.26.ResetState]
stream = "reset.RL_calf_joint"
target = "joint.RL_calf_joint.qpos"

[body.graph.nodes.26.ResetState.dist]
Constant = -1.8

[body.graph.nodes.3.GetJointState]
body = "f0bf6bb9532899c02f517d17d8f290aa"
joints = ["freejoint1"]
quantity = "Position"

[body.graph.nodes.4.Compare]
op = "Gt"
rhs = 3.0

[body.graph.nodes.4.Compare.ty]
elem = "F32"
shape = [1]
time = "Tick"
unit = "Angle"

[body.graph.nodes.4.Compare.ty.frame]
Joint = "f0bf6bb9532899c02f517d17d8f290aa"

[body.graph.nodes.5.Terminate]
kind = "Failure"

[body.graph.nodes.6.GetJointState]
body = "f0bf6bb9532899c02f517d17d8f290aa"
joints = ["freejoint1"]
quantity = "Velocity"

[body.graph.nodes.7.Normalize]
hi = [1.5]
lo = [0.0]
out_hi = 1.0
out_lo = 0.0

[body.graph.nodes.7.Normalize.ty]
elem = "F32"
shape = [1]
time = "Tick"
unit = "AngularVelocity"

[body.graph.nodes.7.Normalize.ty.frame]
Joint = "f0bf6bb9532899c02f517d17d8f290aa"

[body.graph.nodes.8.Reward]
aggregation = "Sum"
name = "forward_velocity"
weight = 1.0

[body.graph.nodes.8.Reward.ty]
elem = "F32"
shape = [1]
time = "Tick"

[body.graph.nodes.8.Reward.ty.frame]
Joint = "f0bf6bb9532899c02f517d17d8f290aa"

[body.graph.nodes.8.Reward.ty.unit.Normalized]
hi = 1.0
lo = 0.0

[body.graph.nodes.9.ActionSpec]
control_rate_hz = 50.0
dim = 12
space = "JointPosition"

[body.observation_spec.channels.state.source.JointState]
body = "f0bf6bb9532899c02f517d17d8f290aa"
dof = 48

[body.observation_spec.channels.state.ty]
elem = "F32"
frame = "World"
shape = [48]
time = "Tick"
unit = "Dimensionless"

[body.scene]
asset_hash = [
    91,
    211,
    225,
    49,
    89,
    68,
    26,
    158,
    124,
    169,
    21,
    226,
    81,
    108,
    111,
    19,
    137,
    139,
    67,
    130,
    227,
    31,
    155,
    176,
    80,
    54,
    81,
    60,
    163,
    109,
    3,
    169,
]
path = "tests/fixtures/mjcf/go1_primitives.xml"
scene_hash = [
    60,
    147,
    72,
    234,
    75,
    225,
    253,
    54,
    34,
    60,
    218,
    71,
    186,
    199,
    180,
    71,
    198,
    223,
    204,
    200,
    49,
    170,
    167,
    210,
    101,
    80,
    226,
    241,
    137,
    196,
    154,
    153,
]
