[package]
name = "es-telemetry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
es-core = { workspace = true }
es-ir = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
# Installs es-core's allocation counter for this crate's tests (P17), so ring.rs can assert
# push() never touches the heap after construction.
es-core = { workspace = true, features = ["alloc-count"] }

[lints]
workspace = true
